import React from 'react'

const Logout = () => {
    return (
        <div>
            <h1 className="logout">
                Welcome <span className="username">Muskaan</span>
                <button className="logout-button">Logout</button>
            </h1>
        </div>
    )
}

export default Logout
